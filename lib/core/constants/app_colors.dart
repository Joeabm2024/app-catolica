import 'package:flutter/material.dart';

/// Paleta inspirada en identidad litúrgica católica:
/// dorado (sagrario), morado (adviento/cuaresma), blanco (pureza), rojo (mártires/Espíritu Santo)
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF6B2C91);      // Morado litúrgico
  static const Color primaryDark = Color(0xFF4A1D66);
  static const Color secondary = Color(0xFFD4AF37);    // Dorado
  static const Color background = Color(0xFFFAF7F2);
  static const Color backgroundDark = Color(0xFF121212);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color error = Color(0xFFB00020);
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF6E6E73);
}